# Content Fallbacks

`site.ts` contains fallback content and route inventory so the site works before Sanity is connected.

Sanity content should override these values at runtime where loaders are wired in.

# Dynamic Top-Level Page

This folder renders top-level SKYPA pages by slug.

It loads Sanity first through `loadSitePage(slug)` and falls back to `getCorePage(slug)`. Keep the page generic and let content drive sections.

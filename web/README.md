# SKYPA Website App

This is the Next.js app for the public SKYPA Foundation website.

The Sanity Studio is intentionally separate in `../studio`. This app only uses Sanity clients and loaders to read content, store form submissions, manage subscribers, and send update notifications through API routes.

Run locally:

```bash
npm install
npm run dev
```
